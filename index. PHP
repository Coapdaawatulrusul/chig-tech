<?php include 'includes/header.php'; ?>
<h2>Welcome to CHIG TECH & IT SOLUTIONS</h2>
<p>Your one-stop destination for tech products, services, training courses, and professional portfolio.</p>
<h3>Featured Products</h3>
<div class="product-grid">
    <?php
    $res = $conn->query("SELECT * FROM products WHERE type='product' ORDER BY created_at DESC LIMIT 4");
    while ($p = $res->fetch_assoc()):
    ?>
    <div class="card">
        <img src="<?= htmlspecialchars($p['image']) ?: 'https://via.placeholder.com/250x150' ?>" alt="">
        <h4><?= htmlspecialchars($p['name']) ?></h4>
        <p><?= htmlspecialchars(substr($p['description'],0,50)) ?>...</p>
        <p><strong>$<?= number_format($p['price'],2) ?></strong></p>
        <a href="public/product_detail.php?id=<?= $p['id'] ?>" class="btn">View Details</a>
    </div>
    <?php endwhile; ?>
</div>
<?php include 'includes/footer.php'; ?>