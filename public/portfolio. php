<?php include '../includes/config.php'; include '../includes/header.php'; ?>
<h2>Our Portfolio</h2>
<div class="product-grid">
    <?php
    $res = $conn->query("SELECT * FROM portfolio ORDER BY id DESC");
    while ($p = $res->fetch_assoc()):
    ?>
    <div class="card">
        <img src="<?= htmlspecialchars($p['image']) ?: 'https://via.placeholder.com/250x150' ?>" alt="">
        <h4><?= htmlspecialchars($p['title']) ?></h4>
        <p><?= htmlspecialchars($p['description']) ?></p>
        <?php if ($p['link']): ?>
            <a href="<?= htmlspecialchars($p['link']) ?>" class="btn" target="_blank">Visit</a>
        <?php endif; ?>
    </div>
    <?php endwhile; ?>
</div>
<?php include '../includes/footer.php'; ?>